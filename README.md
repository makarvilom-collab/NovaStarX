# PixelStars - Telegram Mini App

**PixelStars** — современное Telegram Mini App для покупки и продажи Telegram Stars. Приложение разработано с фокусом на производительность, безопасность и пользовательский опыт.

## 🌟 Особенности

- **Mobile-first дизайн** — оптимизировано для мобильных устройств
- **Полная интеграция с Telegram WebApp API** — нативный опыт внутри Telegram
- **Адаптивный дизайн** — работает на всех устройствах (360×800 до 1024×768+)
- **Безопасные транзакции** — проверка initData и защищённые платежи
- **Локализация** — поддержка русского и английского языков
- **Оптимизированная производительность** — загрузка < 150KB, быстрый рендеринг

## 📱 Скриншоты и дизайн

### Мобильный дизайн (360×800)
```
┌─────────────────────────────────┐
│ ✨ PixelStars                   │
│ Покупай и продавай звёзды       │
│ быстро и безопасно              │
├─────────────────────────────────┤
│ 💎 Текущий курс                 │
│ ┌──────────┬──────────┐         │
│ │  8.50₽   │  7.50₽   │         │
│ │ Покупка  │ Продажа  │         │
│ └──────────┴──────────┘         │
├─────────────────────────────────┤
│ ⭐ Операции со звёздами         │
│ ┌──────────┬──────────┐         │
│ │ 💳 Купить│ 💰 Продать│         │
│ └──────────┴──────────┘         │
│ ┌─────────────────────┐         │
│ │ 📈 История операций │         │
│ └─────────────────────┘         │
├─────────────────────────────────┤
│ Ваша статистика                 │
│ ┌──────────┬──────────┐         │
│ │    0     │    0     │         │
│ │  Звёзд   │ Операций │         │
│ └──────────┴──────────┘         │
└─────────────────────────────────┘
```

## 🚀 Быстрый старт

### Локальный запуск (статические файлы)

```bash
# Клонируйте проект
git clone <repository-url>
cd PixelStars

# Откройте index.html в браузере
open index.html
# или запустите простой HTTP сервер
python3 -m http.server 8000
```

### Запуск с Express сервером

```bash
# Установите зависимости
npm install

# Установите переменные окружения
export BOT_TOKEN="your_telegram_bot_token"

# Запустите сервер
npm start

# Для разработки с автоперезагрузкой
npm run dev
```

## 📁 Структура проекта

```
PixelStars/
├── index.html              # Главная страница приложения
├── package.json            # Зависимости Node.js
├── server.js               # Express сервер (опционально)
├── public/assets/          # Статические ресурсы
│   ├── logo.svg            # Логотип (120×120)
│   ├── favicon.png         # Favicon (32×32)
│   └── icons/              # Иконки
│       ├── buy-24.svg      # Иконка покупки (24×24)
│       ├── sell-24.svg     # Иконка продажи (24×24)
│       ├── history-24.svg  # Иконка истории (24×24)
│       ├── star-24.svg     # Иконка звезды (24×24)
│       ├── buy-32.svg      # Иконка покупки (32×32)
│       └── sell-32.svg     # Иконка продажи (32×32)
├── src/
│   ├── components/         # UI компоненты
│   ├── i18n/               # Файлы локализации
│   │   ├── ru.json         # Русские тексты
│   │   ├── en.json         # Английские тексты
│   │   └── i18n.js         # Система локализации
│   └── api/                # API и моки
│       └── api.js          # Mock API для разработки
└── README.md               # Документация
```

## 🎨 Дизайн-система

### Цветовая палитра
```css
/* Основные цвета */
--primary: #7B68EE;        /* Фиолетовый для звёзд */
--primary-dark: #5A4FCF;   /* Тёмный фиолетовый */
--accent: #FFD700;         /* Золотой для акцентов */
--accent-dark: #FFA500;    /* Тёмное золото */

/* Статусы */
--success: #10B981;        /* Успех */
--danger: #EF4444;         /* Ошибка */
--warning: #F59E0B;        /* Предупреждение */
--info: #3B82F6;           /* Информация */
```

### Типографика
- **Шрифт**: system UI stack (-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Inter')
- **Размеры**: от 12px (0.75rem) до 30px (1.875rem)
- **Высота строки**: 1.5

### Отступы и радиусы
- **Отступы**: 4px, 8px, 16px, 24px, 32px
- **Радиусы**: 6px, 8px, 12px, 16px
- **Тени**: 3 уровня elevation

## 📱 User Flow

### Процесс покупки звёзд
1. **Главная страница** → Пользователь видит текущий курс
2. **Кнопка "Купить"** → Открывается модальное окно
3. **Ввод количества** → Автоматический расчёт суммы
4. **Подтверждение** → Инициализация платежа
5. **Оплата** → Через Telegram Payments или внешний провайдер
6. **Успех** → Обновление баланса, уведомление

### Процесс продажи звёзд
1. **Главная страница** → Проверка баланса
2. **Кнопка "Продать"** → Открывается модальное окно
3. **Ввод количества** → Проверка достаточности средств
4. **Подтверждение** → Блокировка звёзд
5. **Обработка** → Инициация выплаты
6. **Успех** → Списание звёзд, уведомление о выплате

### Обработка ошибок
- **Недостаточно средств** → Показать текущий баланс
- **Неверное количество** → Подсказка о лимитах
- **Ошибка сети** → Кнопка повторить операцию
- **Ошибка платежа** → Детали ошибки, поддержка

## 🔧 Деплой

### Netlify (рекомендуется для статики)

```bash
# 1. Создайте новый сайт в Netlify
# 2. Подключите GitHub репозиторий
# 3. Настройки сборки:
#    Build command: (оставить пустым)
#    Publish directory: .
# 4. Добавьте переменные окружения в Netlify UI

# Файл netlify.toml (создать в корне)
[build]
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "ALLOWALL"
    X-Content-Type-Options = "nosniff"
```

### Vercel (альтернатива)

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Настройте переменные окружения
vercel env add BOT_TOKEN
```

### Heroku (для Node.js сервера)

```bash
# Создайте Heroku приложение
heroku create pixelstars-app

# Установите переменные окружения
heroku config:set BOT_TOKEN=your_bot_token

# Деплой
git push heroku main
```

## 🔐 Безопасность

### Проверка initData
```javascript
// Сервер должен проверять подпись initData
function verifyTelegramWebAppData(initData) {
    const hash = urlParams.get('hash');
    const secretKey = crypto.createHmac('sha256', 'WebAppData').update(BOT_TOKEN).digest();
    const calculatedHash = crypto.createHmac('sha256', secretKey).update(dataCheckString).digest('hex');
    return calculatedHash === hash;
}
```

### Настройки безопасности
- ✅ Никогда не храните BOT_TOKEN в клиентском коде
- ✅ Проверяйте initData на сервере
- ✅ Используйте HTTPS для всех запросов
- ✅ Валидируйте все входящие данные
- ✅ Логируйте все транзакции

## 🧪 Тест-кейсы

### 1. Проверка корректного initData
```javascript
// Тест: Корректные данные от Telegram
assert(verifyInitData(validInitData) === true);
```

### 2. Обработка отказа оплаты
```javascript
// Тест: Пользователь отменил платёж
simulatePaymentCancellation();
assert(userBalance === initialBalance);
```

### 3. Адаптивность интерфейса
```javascript
// Тест: Проверка на разных размерах экрана
testViewports([
    { width: 360, height: 800 },  // Android
    { width: 375, height: 812 },  // iPhone X
    { width: 412, height: 915 },  // Android wide
    { width: 1024, height: 768 }  // Tablet
]);
```

### 4. Производительность загрузки
```javascript
// Тест: Время загрузки < 3 секунд
assert(loadTime < 3000);
assert(totalJSSize < 150 * 1024); // < 150KB
```

### 5. Локализация
```javascript
// Тест: Переключение языков
i18n.setLanguage('en');
assert(document.title === 'PixelStars');
```

### 6. Валидация форм
```javascript
// Тест: Неверные данные в формах
assert(validateAmount(-5) === false);
assert(validateAmount(0) === false);
assert(validateAmount(10001) === false);
```

### 7. Обработка сетевых ошибок
```javascript
// Тест: Потеря сети во время транзакции
simulateNetworkError();
assert(transactionStatus === 'pending');
```

### 8. Интеграция с Telegram WebApp
```javascript
// Тест: MainButton функциональность
Telegram.WebApp.MainButton.setText('Test');
assert(Telegram.WebApp.MainButton.text === 'Test');
```

## 📄 Экспортируемые ассеты

### Логотипы и иконки
- `logo.svg` (120×120) — Основной логотип
- `favicon.png` (32×32) — Favicon для браузера
- `app-icon-64.png` (64×64) — Иконка приложения

### Иконки интерфейса (24×24)
- `buy-24.svg` — Иконка покупки
- `sell-24.svg` — Иконка продажи  
- `history-24.svg` — Иконка истории
- `star-24.svg` — Иконка звезды

### Иконки интерфейса (32×32)
- `buy-32.svg` — Иконка покупки (большая)
- `sell-32.svg` — Иконка продажи (большая)

### Hero изображения
- `hero.jpg` (720×400) — Баннер для веб-версии
- `hero@2x.jpg` (1440×800) — Баннер для Retina дисплеев

## 🌍 Локализация

### Поддерживаемые языки
- **Русский (ru)** — основной язык
- **Английский (en)** — дополнительный

### Добавление нового языка
1. Создайте файл `src/i18n/{lang}.json`
2. Скопируйте структуру из `ru.json`
3. Переведите все строки
4. Добавьте язык в `i18n.js`

## 🔄 API Endpoints

### GET /api/rates
Получение текущих курсов валют
```json
{
  "success": true,
  "data": {
    "buy": 8.50,
    "sell": 7.50,
    "lastUpdate": 1640995200000
  }
}
```

### GET /api/user
Получение данных пользователя (требует авторизации)
```json
{
  "success": true,
  "data": {
    "id": 123456789,
    "balance": 500,
    "totalTransactions": 15
  }
}
```

### POST /api/buy
Покупка звёзд
```json
{
  "amount": 100,
  "total": 850.00
}
```

### POST /api/sell  
Продажа звёзд
```json
{
  "amount": 100,
  "total": 750.00
}
```

## 📞 Поддержка

### Разработка
- GitHub Issues для багов и предложений
- Документация API в коде
- Логирование ошибок в консоль

### Продакшен
- Telegram бот поддержки
- Email: support@pixelstars.app
- Мониторинг системы 24/7

---

**PixelStars** © 2024. Создано с ❤️ для Telegram экосистемы.